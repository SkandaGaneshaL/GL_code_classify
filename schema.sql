-- Run this script in the Oracle schema used by the existing POC .env file.
-- ACCOUNT_TYPE is populated from SEGMENT3_DESCRIPTION in the XML history.

CREATE TABLE AG_GL_ACCOUNT_HISTORY_CASES (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    SOURCE_KEY VARCHAR2(64 CHAR) NOT NULL,
    INVOICE_ID VARCHAR2(200 CHAR),
    INVOICE_NUM VARCHAR2(200 CHAR) NOT NULL,
    INVOICE_DATE VARCHAR2(80 CHAR),
    INVOICE_DISTRIBUTION_ID VARCHAR2(200 CHAR),
    INVOICE_LINE_NUMBER NUMBER,
    DISTRIBUTION_LINE_NUMBER NUMBER,
    DIST_CODE_COMBINATION_ID VARCHAR2(200 CHAR),
    LINE_DESCRIPTION VARCHAR2(4000 CHAR) NOT NULL,
    LINE_TYPE VARCHAR2(100 CHAR),
    VENDOR_ID VARCHAR2(200 CHAR),
    VENDOR_SITE_ID VARCHAR2(200 CHAR),
    BUSINESS_UNIT_ID VARCHAR2(200 CHAR),
    LEDGER_ID VARCHAR2(200 CHAR),
    CHART_OF_ACCOUNTS_ID VARCHAR2(200 CHAR),
    ACCOUNT_TYPE VARCHAR2(200 CHAR) NOT NULL,
    GL_CODE VARCHAR2(200 CHAR) NOT NULL,
    SEGMENT1 VARCHAR2(100 CHAR),
    SEGMENT2 VARCHAR2(100 CHAR),
    SEGMENT3 VARCHAR2(100 CHAR),
    SEGMENT4 VARCHAR2(100 CHAR),
    SEGMENT5 VARCHAR2(100 CHAR),
    SEGMENT6 VARCHAR2(100 CHAR),
    EMBEDDING VECTOR(1024, FLOAT32) NOT NULL,
    DATASET_TYPE VARCHAR2(100 CHAR),
    IS_SYNTHETIC VARCHAR2(20 CHAR),
    SOURCE_INVOICE_DISTRIBUTION_ID VARCHAR2(200 CHAR),
    SYNTHETIC_TYPE VARCHAR2(100 CHAR),
    CREATED_AT TIMESTAMP DEFAULT SYSTIMESTAMP NOT NULL,
    CONSTRAINT UK_AG_GL_ACCOUNT_HISTORY_KEY UNIQUE (SOURCE_KEY)
);

CREATE INDEX IDX_AG_GL_ACCOUNT_HISTORY_TYPE
    ON AG_GL_ACCOUNT_HISTORY_CASES (ACCOUNT_TYPE);

CREATE VECTOR INDEX IDX_AG_GL_ACCOUNT_HISTORY_EMB
    ON AG_GL_ACCOUNT_HISTORY_CASES (EMBEDDING)
    ORGANIZATION INMEMORY NEIGHBOR GRAPH
    DISTANCE COSINE
    WITH TARGET ACCURACY 95;
